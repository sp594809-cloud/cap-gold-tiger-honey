// Compatibility entrypoint for the CMS server functions.
// The implementation lives in cms.ts; keep this alias for routes and components
// that import the server-function module by its explicit .server name.
export * from "./cms";
